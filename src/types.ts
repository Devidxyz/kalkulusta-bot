type PendingRating = {
  character?: string;
  channelId?: string;
  subject?: string;
  text?: string;
  aspects?: number[];
  sexy?: boolean;
};

// eslint-disable-next-line import/prefer-default-export
export { PendingRating };
